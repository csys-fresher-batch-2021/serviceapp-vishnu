create table user_details(
	first_name varchar(20),
	last_name varchar(20),
	contact bigint,
	email varchar(50),
	password varchar(50)
);

create table cars(
	available_cars(30)
);

create table services(
	available_services varchar(20)
);

create table service_centers_db(
	center_name varchar(100),
	location varchar(50)
);